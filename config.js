/* Shared frontend config. Every page that talks to the backend reads API_BASE from here —
   change this one line to point the whole site at a different backend deployment. */
const API_BASE = "http://localhost:4000";
